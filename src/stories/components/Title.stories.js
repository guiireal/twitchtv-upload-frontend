import React from "react";
import Title from "../../components/Title";

const parameters = {
  title: "Components/Title",
  component: Title,
  properties: {
    layout: "centered",
  },
};

export default parameters;

export const Default = () => <Title>Profile</Title>;
